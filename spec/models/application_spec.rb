require 'spec_helper'

describe Application do
  # it "does not have a token before save" do
  #   app = Application.new(name: "Test App")
  #   expect(app.token).to be_nil
  # end

  # it "does not have a token on create" do
  #   app = Application.create!(name: "Test App")
  #   expect(app.token).to be_nil
  # end

  # it "has a token after create" do
  #   app = Application.new(name: "Test App")
  #   app.save
  #   expect(app.token).to_not be_nil
  # end

  # it "doesn't allow the token to be changed" do
  #   app = Application.create(name: "Test App")
  #   expect{app.token = "Test"}.to raise_error
  # end
end